[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pylblrtm"
version = "0.1.0"
description = "Line-by-line radiative transfer model for atmospheric remote sensing"
readme = "DOCS.md"
license = {text = "MIT"}
requires-python = ">=3.9"

dependencies = [
    "numpy>=1.24",
]

[project.optional-dependencies]
hapi = ["hitran-api>=1.3"]   # necesario solo para from_hapi() y descarga HITRAN
plot = ["matplotlib>=3.7"]
dev  = ["pytest>=7", "sphinx>=7", "sphinx-rtd-theme"]

[project.urls]
Repository = "https://github.com/tu-usuario/pylblrtm"

[tool.setuptools.packages.find]
where = ["."]
include = ["pylblrtm*"]
